<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heybox.test_new">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.RadioGroupActivity" />
        <activity android:name=".activities.FragmentActivity" />
        <activity android:name=".activities.DialogActivity" />
        <activity android:name=".activities.BroadActivity1"  android:launchMode="singleTask">

        </activity>
        <activity android:name=".activities.BroadActivity2"  android:launchMode="singleTask"/>

        <activity android:name=".activities.BulrActivity" />
        <activity android:name=".TestActivityClip" />
        <activity android:name=".activities.SpannableActivity" />
        <activity android:name=".activities.ChangeableActivity"></activity>
        <activity android:name=".activities.TabLayoutActivity">

        </activity>
        <activity android:name=".activities.StikyRecyclerActivity"></activity>
        <activity android:name=".activities.ViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>