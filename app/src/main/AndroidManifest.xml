<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heybox.test_new">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".RadioGroupActivity" />
        <activity android:name=".FragmentActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".BroadActivity1" />
        <activity android:name=".BroadActivity2" />

        <receiver android:name=".MyReceiver">
            <intent-filter>
                <action android:name="receiver" />
            </intent-filter>
        </receiver>

        <activity android:name=".BulrActivity"></activity>
        <activity android:name=".BitmapActivity">

        </activity>
        <activity android:name=".TestActivityClip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>